type BalanceAccount @entity {
  "Tea token address"
  id: ID!
  createdAt: BigInt!
  balance: BigInt!
}

type User @entity {
  "Tea token address"
  id: ID!
  balance: BigInt!
  userName: String!
  createdAt: BigInt!
}