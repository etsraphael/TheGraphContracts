{
  "name": "the-graph-contracts",
  "license": "UNLICENSED",
  "scripts": {
    "test": "graph codegen && graph build",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ wiinscore",
    "create-local": "graph create --node http://localhost:8020/ wiinscore",
    "remove-local": "graph remove --node http://localhost:8020/ wiinscore",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 wiinscore"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
