type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  owner: Bytes! # address
  spender: Bytes! # address
}

type User @entity {
  id: ID!
  balance: BigInt!
  userName: String!
  createdAt: BigInt!
}